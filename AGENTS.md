# Repository Working Rules

이 저장소에서 구현 작업을 수행하는 모든 에이전트는 아래 절차를 따른다.

## 0. 작업의 목적과 사용자 가치를 먼저 설명한다

- 구현 방법이나 파일 목록보다 먼저 **왜 이 작업을 하는지**를 평문으로 설명한다.
- 작업을 시작할 때 다음 질문에 답하고 작업 문서의 `목적과 사용자 가치` 섹션에 기록한다.
  - 누가 어떤 문제를 겪고 있는가?
  - 작업이 끝나면 사용자 또는 운영자가 무엇을 할 수 있게 되는가?
  - 이 작업은 상위 GitHub Issue, story, milestone 또는 release 목표에서 어떤 역할을 맡는가?
  - 선행 작업 중 무엇을 이어받으며, 왜 다른 후보보다 지금 수행해야 하는가?
- 최종 사용자에게 직접 보이는 변화가 없는 리팩터링, 테스트, CI, 계약 또는 문서 작업은 어떤 장애·오판·
  회귀 위험을 줄이는지 설명한다. “기반 작업”, “안전망” 같은 표현만 쓰지 말고 그 기반이 어떤 후속 사용자
  결과를 가능하게 하는지 적는다.
- 작업 이름, branch 이름과 commit 메시지는 가능하면 구현 수단보다 달성하려는 결과를 드러내게 한다.
- 목적을 설명할 수 없거나 상위 목표와 연결되지 않는 작업은 바로 구현하지 않는다. 먼저 필요성과 우선순위를
  사용자에게 확인한다.

## 1. 개발 branch를 main과 분리한다

- 파일을 변경하기 전에 `git branch --show-current`와 `git status --short --branch`로 현재 branch와
  worktree를 확인한다.
- `main` 또는 `master`에서는 문서, 코드, 설정을 포함한 어떤 파일도 변경하지 않는다. commit과 push도
  금지한다.
- 작업은 반드시 목적이 드러나는 전용 개발 branch에서 수행한다. 기본 이름은
  `feat/<task>`, `fix/<task>`, `docs/<task>`, `refactor/<task>`, `test/<task>`, `chore/<task>` 또는
  `release/<version>` 형식을 사용한다.
- 작업 시작 시 `main`/`master`에 있다면 파일을 변경하기 전에 최신 `origin/main`을 기준으로 개발 branch를
  생성·전환한다. 안전하게 분리할 수 없는 기존 변경이 있으면 임의로 옮기지 말고 사용자에게 보고한다.
- 개발 branch의 변경은 동일 이름의 원격 branch에만 push한다. `main`/`master` 반영은 Pull Request와
  승인된 merge 절차로만 수행한다.
- force push, history rewrite와 원격 branch 삭제는 사용자의 명시적 요청 없이는 수행하지 않는다.

## 2. 구현 전에 계획을 문서화한다

- 코드 또는 설정을 변경하기 전에 작업 문서를 먼저 만든다.
- 작업 문서는 `docs/work/<issue-or-task>-<short-name>.md` 경로를 사용한다.
  - GitHub Issue가 있으면 번호를 포함한다. 예: `docs/work/issue-3-graph-usability.md`
  - Issue가 없으면 작업을 식별할 수 있는 짧은 kebab-case 이름을 사용한다.
- 작업 문서에는 최소한 다음 내용을 기록한다.
  - 목적과 사용자 가치
  - 배경과 해결할 문제
  - 범위와 범위에서 제외할 항목
  - 현재 구현 조사 결과
  - 단계별 구현 계획
  - 테스트 및 완료 기준
- `목적과 사용자 가치`는 기술 용어를 모르는 저장소 소유자도 이해할 수 있어야 한다. 기능 이름이나 내부
  모듈을 나열하는 것으로 대신하지 않는다.
- `현재 구현 조사 결과`에는 상위 목표 대비 이미 달성된 사용자 결과, 아직 불가능한 사용자 결과와 이번
  작업이 메우는 공백을 구분해 기록한다.
- `단계별 구현 계획`의 최상위 단계는 각각 독립적으로 검증·commit·push 가능한 단위로 작성한다.
  독립적으로 정상 상태를 만들 수 없는 단계들은 구현 전에 하나의 단계로 합친다.
- 각 최상위 단계에는 최소한 다음 내용을 포함한다.
  - 목적: 이 단계를 왜 수행하는가
  - 산출물: 완료되면 무엇이 달라지는가
  - 검증: 그 결과를 어떤 관측이나 테스트로 증명하는가
- 조사 과정에서 계획이 틀렸거나 범위가 달라진 것을 발견하면 구현을 계속하기 전에 문서를 갱신한다.

## 3. 작업 내역과 목적의 달성 여부를 지속적으로 기록한다

- 같은 작업 문서에 `작업 로그` 섹션을 두고 구현 중 변경 사항을 계속 기록한다.
- 로그에는 최소한 다음 항목을 남긴다.
  - 변경한 파일과 핵심 변경 내용
  - 중요한 설계 결정과 선택 이유
  - 실행한 검사 또는 테스트와 결과
  - 이 단계가 사용자 결과 또는 상위 목표에 실제로 기여한 내용
  - 발견한 제한 사항, 남은 작업, 후속 과제
- 단순히 "완료"라고 쓰지 말고, 나중에 변경 이유와 검증 근거를 재구성할 수 있도록 구체적으로 작성한다.
- 계획과 실제 구현이 달라졌다면 차이와 이유를 명시한다.
- 구현은 끝났지만 사용자 결과가 아직 달성되지 않았다면 “완료”로 뭉뚱그리지 않는다. 완료된 기술 조건과
  남은 사용자 검증·통합·배포 조건을 분리한다.

### Handover 작성 규칙

- handover는 branch, PR, commit 표보다 먼저 `사용자 관점 요약`을 둔다.
- `사용자 관점 요약`에는 다음 내용을 평문으로 기록한다.
  - 지금까지 해결된 사용자 문제와 새로 가능해진 행동
  - 아직 해결되지 않아 사용자가 할 수 없는 것 또는 오해할 수 있는 것
  - 다음 작업의 목적과 그것이 지금 다음 순서인 이유
- 기술 상태에는 각 PR과 commit이 **무엇을 위한 변경이었는지**를 한 문장으로 함께 적는다.
- 다음 작업 순서는 branch 이름이나 모듈 이름만 나열하지 않고, 각 단계가 최종 사용자 결과에 어떻게
  이어지는지 설명한다.

## 4. 완료 여부를 검증한다

- 작업 문서의 완료 기준을 모두 확인한다.
- 변경 범위에 맞는 컴파일, 테스트, 린트 또는 패키징 검증을 실행한다.
- 검증할 수 없는 항목은 성공으로 간주하지 말고 이유와 위험을 작업 로그에 기록한다.
- 관련 테스트가 실패하거나 필수 요구사항이 남아 있으면 작업을 완료로 표시하지 않는다.
- 테스트 통과만으로 사용자 목적이 달성됐다고 간주하지 않는다. 작업 문서의 `목적과 사용자 가치`에서 약속한
  결과가 실제로 관측됐는지 확인하고, 사용자 검증이나 배포가 별도로 남으면 그 경계를 기록한다.

## 5. 각 구현 단계 종료 시 commit하고 push한다

- 작업 문서의 각 최상위 구현 단계가 끝날 때마다 다음 순서를 모두 수행한다.
  1. 단계 범위에 맞는 필수 검증을 실행한다.
  2. 작업 문서 로그와 완료 기준을 실제 결과로 갱신한다.
  3. `git status`, staged/unstaged diff와 `git diff --check`를 확인한다.
  4. 해당 단계의 구현, 테스트와 문서만 하나의 독립 commit으로 남긴다.
  5. 현재 개발 branch와 동일 이름의 원격 branch로 push한다. 첫 push라면 upstream을 설정한다.
  6. local HEAD가 upstream에 반영되고 worktree에 의도하지 않은 변경이 없는지 확인한다.
- commit과 원격 push가 성공하기 전에는 해당 단계를 완료로 표시하거나 다음 구현 단계로 진행하지 않는다.
- push가 실패하면 해당 단계는 `In progress`로 유지하고 원인을 조사·보고한다. 실패를 숨기고 다음 단계로
  넘어가거나 다른 branch, 특히 `main`/`master`에 우회 push하지 않는다.
- 커밋 전 `git status`와 diff를 확인해 사용자 또는 다른 작업자의 변경을 포함하지 않는다.
- 커밋에는 구현 파일, 테스트, 그리고 해당 작업 문서를 함께 포함한다.
- 커밋 메시지는 변경 의도를 설명하는 명령형 문구로 작성한다. GitHub Issue가 있으면 가능할 때 Issue 번호를 포함한다.
- 서로 독립적인 변경은 별도 커밋으로 분리한다.
- Pull Request를 만들 때 본문 첫 부분에 작업 목적, 기대한 사용자·운영 결과와 상위 목표 연결을 적는다.
  구현 파일과 test 결과만 나열하지 않으며, 이 PR만으로 아직 달성되지 않는 결과가 있으면 명시한다.
- 다음 경우에는 임의로 커밋하지 않는다.
  - 사용자가 커밋하지 말라고 요청한 경우
  - 작업이 미완료이거나 필수 검증이 실패한 경우
  - 포함해야 할 변경과 사용자 소유 변경을 안전하게 구분할 수 없는 경우
- 커밋하지 못한 경우 그 이유와 현재 상태를 사용자에게 보고한다.
- 검증 실패나 미완료 상태 때문에 commit할 수 없는 경우 push도 하지 않는다. 먼저 작업 문서에서 단계 범위와
  상태를 갱신하고 안전한 해결 또는 사용자 결정을 기다린다.

## 6. 완료 보고에 목적과 근거를 포함한다

- 최종 보고는 “무엇을 변경했다”보다 “어떤 문제를 해결했고 무엇이 가능해졌는가”를 먼저 설명한다.
- 최종 보고에는 달성한 사용자 또는 운영 결과, 상위 목표에 대한 기여, 작업 문서 경로, 주요 구현 내용,
  검증 결과와 commit hash를 포함한다.
- 단계별 commit hash와 push된 원격 개발 branch를 포함한다.
- 남은 제한 사항이나 후속 작업이 있으면 사용자가 아직 할 수 없는 것과 다음 작업이 필요한 이유를 함께
  명시한다.
