# Claude Code Repository Instructions

Claude Code로 이 저장소를 조사하거나 변경할 때 root의 [`AGENTS.md`](AGENTS.md)를 전체 절차의 단일 기준으로
따른다. 이 문서는 그 절차를 복제하지 않고 Claude Code가 특히 놓치기 쉬운 목적 중심 계획·보고 원칙을
강조한다.

## 목적부터 설명한다

- code, branch, PR, commit, wave 또는 내부 모듈을 설명하기 전에 사용자가 겪는 문제와 원하는 결과를 먼저
  평문으로 설명한다.
- 모든 작업 문서에 `목적과 사용자 가치`를 두고 사용자 문제, 완료 후 가능해지는 행동, 상위 목표와의 관계,
  지금 이 작업을 수행하는 이유를 기록한다.
- 리팩터링, 계약, 테스트, CI와 문서 작업은 “내부 개선”으로 끝내지 않는다. 어떤 실패, 오판 또는 회귀를
  방지하고 어떤 후속 사용자 기능을 가능하게 하는지 명시한다.
- 기술 단계는 각각 `목적`, `산출물`, `검증`으로 설명한다. 구현 파일 목록은 산출물이 아니다.

## 진행 상황을 사용자 결과로 번역한다

- 중간 보고와 완료 보고는 달성한 사용자·운영 결과를 먼저 말하고 그 근거로 test, PR과 commit을 제시한다.
- Pull Request 본문도 목적과 기대 사용자 결과를 먼저 설명하고, 이 PR 이후에도 남는 결과를 구분한다.
- “구현 완료”, “CI 성공”, “merge 완료”를 사용자 목표 달성과 같은 뜻으로 사용하지 않는다. 통합, 배포,
  독립 검토 또는 실제 사용자 검증이 남았다면 분리해 적는다.
- 약어와 lane 이름을 처음 사용할 때 그것이 해결하는 문제를 함께 설명한다. 사용자가 기존 handover를 모두
  읽었다고 가정하지 않는다.

## Handover와 재개

- handover 첫 부분에 `사용자 관점 요약`을 두고 다음 세 가지를 답한다.
  1. 지금까지 어떤 문제가 해결됐고 무엇이 가능해졌는가?
  2. 아직 무엇이 불가능하거나 오해될 수 있는가?
  3. 다음 작업은 무엇을 위한 것이며 왜 지금 해야 하는가?
- PR·commit·worktree 표에는 각 항목의 목적을 한 문장으로 붙인다.
- 재개할 때는 최신 handover의 기술 순서를 그대로 따르기 전에 현재 사용자 목표와 원격 상태가 여전히 맞는지
  확인한다. 달라졌다면 구현 전에 작업 문서를 갱신한다.

## 변경 절차

- branch 분리, 사전 작업 문서, 작업 로그, 검증, 단계별 commit·push와 완료 보고는 모두 `AGENTS.md`를
  따른다.
- `main` 또는 `master`에서 파일을 변경하지 않는다.
- 목적과 우선순위를 설명할 수 없는 새 범위는 임의로 추가하지 않고 사용자에게 확인한다.
